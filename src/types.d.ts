type SideBarProps = {
    sideBarClass: string;
  };

type HeaderProps = {
  userName: string;
  tag: string;
};

type AboutRow = {
  id: number;
  heading: string;
  description: string;
  status: string;
};

type ServiceRow = {
  id: number;
  heading: string;
  description: string;
  icon: string;
}